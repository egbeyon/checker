

1. connected to the servers with ssh through a login playbook
   
2. pinged the servers to verify the connection

3. executed the playbook

4. verified if the configuration were implemented

5. edited the playbook to include users and group ownerships and permissions of specific directories in each application server.

6. executed and verified the implementation of the playbook through the jumphost, with "shell -a" commands
   
